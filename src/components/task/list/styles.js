import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    listContainer: {
        marginHorizontal: 20,

    },
    listTitle: {
        fontSize: 18,
        fontWeight: 'bold',
        color: '#212121',
        marginBottom: 10,

    },
})